{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE"
  },

  "deploy": {
    "startCommand": "gunicorn medicart.wsgi:application --bind 0.0.0.0:$PORT --workers 4 --timeout 120",
    "restartPolicyMaxRetries": 5
  }

}
