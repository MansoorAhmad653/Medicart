{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE"
  },
  "deploy": {
    "startCommand": "python manage.py migrate --noinput && gunicorn medicart.wsgi --bind 0.0.0.0:${PORT:-8000}",
    "restartPolicyMaxRetries": 5
  }
}
